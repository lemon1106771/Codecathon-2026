{
  "name": "tamago-app",
  "version": "1.0.0",
  "description": "Stranger-matching chat based on taste compatibility",
  "main": "server/index.js",
  "scripts": {
    "setup": "npm run install:server && npm run install:client",
    "install:server": "cd server && npm install --production=false",
    "install:client": "cd client && npm install --production=false",
    "build:client": "cd client && npm run build",
    "build": "npm run install:server && npm run install:client && npm run build:client",
    "start": "cd server && npm start",
    "dev:server": "cd server && npm start",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
